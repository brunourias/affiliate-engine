import type {Settings,Approval,Notification,AgentTask,Decision,Dashboard,Health,MarketplaceConnection,MarketplaceCapability,MarketplaceCategory,RadarRun,RadarSignal,RadarStatus,Candidate,Evidence,Checklist} from '../types';
import { apiErrorMessage } from '../lib/apiError';
const BASE=import.meta.env.VITE_API_URL??'http://127.0.0.1:8000/api/v1';
async function request<T>(path:string,init?:RequestInit):Promise<T>{const response=await fetch(`${BASE}${path}`,{...init,headers:{'Content-Type':'application/json',...init?.headers}});if(!response.ok){const body:unknown=await response.json().catch(()=>null);throw new Error(apiErrorMessage(body,response.status))}return response.json() as Promise<T>}
export const api={candidates:()=>request<Candidate[]>('/curator/candidates'),candidate:(id:string)=>request<Candidate>('/curator/candidates/'+id),createCandidate:(body:Record<string,unknown>)=>request<Candidate>('/curator/candidates',{method:'POST',body:JSON.stringify(body)}),updateCandidate:(id:string,body:Record<string,unknown>)=>request<Candidate>('/curator/candidates/'+id,{method:'PATCH',body:JSON.stringify(body)}),investigate:(id:string)=>request<Candidate>('/curator/candidates/from-radar/'+id,{method:'POST'}),evidence:(id:string)=>request<Evidence[]>('/curator/candidates/'+id+'/evidence'),addEvidence:(id:string,body:Record<string,unknown>)=>request<Evidence>('/curator/candidates/'+id+'/evidence',{method:'POST',body:JSON.stringify(body)}),checklist:(id:string)=>request<Checklist>('/curator/candidates/'+id+'/checklist'),archiveCandidate:(id:string)=>request<Candidate>('/curator/candidates/'+id+'/archive',{method:'POST'}),reopenCandidate:(id:string)=>request<Candidate>('/curator/candidates/'+id+'/reopen',{method:'POST'}),dashboard:()=>request<Dashboard>('/dashboard'),health:()=>request<Health>('/health'),settings:()=>request<Settings>('/settings'),patchSettings:(body:Partial<Settings>)=>request<Settings>('/settings',{method:'PATCH',body:JSON.stringify(body)}),pause:()=>request<Settings>('/automation/pause',{method:'POST'}),resume:()=>request<Settings>('/automation/resume',{method:'POST'}),approvals:(status='')=>request<Approval[]>(`/approvals${status?`?status=${status}`:''}`),decide:(id:string,decision:'approve'|'reject',reason='')=>request<Approval>(`/approvals/${id}/${decision}`,{method:'POST',body:JSON.stringify({reason:reason||null})}),notifications:()=>request<Notification[]>('/notifications'),read:(id:string)=>request<Notification>(`/notifications/${id}/read`,{method:'POST'}),readAll:()=>request<{updated:number}>('/notifications/read-all',{method:'POST'}),decisions:()=>request<Decision[]>('/decisions'),tasks:()=>request<AgentTask[]>('/tasks'),createTask:(body:{type:string;title:string;isAutomatic:boolean})=>request<AgentTask>('/tasks',{method:'POST',body:JSON.stringify(body)}),runTask:(id:string)=>request<AgentTask>(`/tasks/${id}/run`,{method:'POST'}),cancelTask:(id:string)=>request<AgentTask>(`/tasks/${id}/cancel`,{method:'POST'}),marketplace:()=>request<MarketplaceConnection>('/marketplaces/mercado-livre'),marketplaceCapabilities:()=>request<MarketplaceCapability[]>('/marketplaces/mercado-livre/capabilities'),runMarketplaceDiagnostics:(categoryId:string)=>request<MarketplaceConnection>('/marketplaces/mercado-livre/diagnostics',{method:'POST',body:JSON.stringify({categoryId})}),runMarketplaceItemDiagnostics:(item:string)=>request<MarketplaceConnection>('/marketplaces/mercado-livre/diagnostics/item',{method:'POST',body:JSON.stringify({item})}),radarStatus:()=>request<RadarStatus>('/radar/mercado-livre/status'),radarCategories:()=>request<MarketplaceCategory[]>('/radar/mercado-livre/categories'),syncRadarCategories:()=>request<{fetched:number;inserted:number;updated:number}>('/radar/mercado-livre/categories/sync',{method:'POST'}),radarRuns:()=>request<RadarRun[]>('/radar/mercado-livre/runs'),createRadarRun:(categoryId:string|null)=>request<RadarRun>('/radar/mercado-livre/runs',{method:'POST',body:JSON.stringify({categoryId})}),radarSignals:(runId:string)=>request<RadarSignal[]>(`/radar/mercado-livre/runs/${runId}/signals`)};
