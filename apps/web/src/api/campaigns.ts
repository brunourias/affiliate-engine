import type{Approval,Campaign,CampaignAngle,CampaignChannel,CampaignExperiment,CampaignReadiness}from'../types';
import{apiErrorMessage}from'../lib/apiError';
const BASE=import.meta.env.VITE_API_URL??'http://127.0.0.1:8000/api/v1';async function request<T>(path:string,init?:RequestInit):Promise<T>{const response=await fetch(`${BASE}${path}`,{...init,headers:{'Content-Type':'application/json'}});if(!response.ok){const body:unknown=await response.json().catch(()=>null);throw new Error(apiErrorMessage(body,response.status))}return response.json() as Promise<T>}
export const campaignsApi={list:()=>request<Campaign[]>('/campaigns'),get:(id:string)=>request<Campaign>('/campaigns/'+id),create:(assessmentId:string,name:string)=>request<Campaign>('/campaigns/from-assessment/'+assessmentId,{method:'POST',body:JSON.stringify({assessmentId,name})}),patch:(id:string,body:Record<string,unknown>)=>request<Campaign>('/campaigns/'+id,{method:'PATCH',body:JSON.stringify(body)}),channels:(id:string)=>request<CampaignChannel[]>('/campaigns/'+id+'/channels'),addChannel:(id:string,body:Record<string,unknown>)=>request<CampaignChannel>('/campaigns/'+id+'/channels',{method:'POST',body:JSON.stringify(body)}),angles:(id:string)=>request<CampaignAngle[]>('/campaigns/'+id+'/angles'),addAngle:(id:string,body:Record<string,unknown>)=>request<CampaignAngle>('/campaigns/'+id+'/angles',{method:'POST',body:JSON.stringify(body)}),experiments:(id:string)=>request<CampaignExperiment[]>('/campaigns/'+id+'/experiments'),addExperiment:(id:string,body:Record<string,unknown>)=>request<CampaignExperiment>('/campaigns/'+id+'/experiments',{method:'POST',body:JSON.stringify(body)}),readiness:(id:string)=>request<CampaignReadiness>('/campaigns/'+id+'/readiness'),submit:(id:string)=>request<Approval>('/campaigns/'+id+'/submit-for-approval',{method:'POST'})};
