import {useCallback}from'react';import{api}from'../api/client';import{useLoad}from'../hooks';import{Badge,Section,Loading,ErrorState,money,date,statusTone}from'../components/ui';import{Power,TriangleAlert,CheckCircle2,Clock3,DatabaseZap}from'lucide-react';
export function Dashboard(){const loader=useCallback(()=>api.dashboard(),[]);const{data,error,loading,refresh}=useLoad(loader);if(loading)return <Page><Loading/></Page>;if(error||!data)return <Page><ErrorState error={error??new Error('Sem dados')} retry={refresh}/></Page>;const toggle=async()=>{data.settings.systemAutomationEnabled?await api.pause():await api.resume();refresh()};const pct=Math.min(100,Math.round(data.commission.confirmedCents/Math.max(1,data.commission.goalCents)*100));return <Page><div className="priority-grid"><Section title="Estado do sistema" aside={<Badge tone={data.settings.systemAutomationEnabled?'success':'danger'}>{data.settings.systemAutomationEnabled?'AUTOMAÇÃO ATIVA':'PAUSADA'}</Badge>}><div className="system-line"><div><span className={`status-light ${data.settings.systemAutomationEnabled?'on':'off'}`}/><b>{data.settings.systemAutomationEnabled?'Operação normal':'Kill Switch ativo'}</b><p>{data.settings.systemAutomationEnabled?'Scheduler autorizado a executar tarefas automáticas dentro dos limites.':'Tarefas automáticas permanecem na fila e não podem iniciar.'}</p></div><button className={data.settings.systemAutomationEnabled?'danger-button':'primary-button'} onClick={toggle}><Power size={18}/>{data.settings.systemAutomationEnabled?'Parar toda automação':'Reativar automação'}</button></div></Section><Section title="Ação necessária"><div className="metric-row"><div><strong>{data.pendingApprovals}</strong><span>aprovações pendentes</span></div><div><strong>{data.unreadNotifications}</strong><span>alertas não lidos</span></div><div><strong>{data.agent.failed}</strong><span>tarefas com falha</span></div></div></Section></div><div className="dashboard-grid"><Section title="Meta mensal"><div className="goal"><div><strong>{money(data.commission.confirmedCents)}</strong><span> de {money(data.commission.goalCents)}</span></div><div className="progress"><i style={{width:`${pct}%`}}/></div><p><DatabaseZap size={16}/>Fonte de dados ainda não conectada</p></div></Section><Section title="Agente"><dl className="facts"><div><dt>Status</dt><dd><Badge tone={data.agent.status==='PAUSED'?'danger':'success'}>{data.agent.status}</Badge></dd></div><div><dt>Tarefa atual</dt><dd>{data.agent.currentTask?.title??'Nenhuma'}</dd></div><div><dt>Pendentes / Falhas</dt><dd>{data.agent.pending} / {data.agent.failed}</dd></div><div><dt>Última execução</dt><dd>{date(data.agent.lastExecution)}</dd></div></dl></Section><Section title="Aprovações recentes" aside={<a href="/aprovacoes">Ver todas</a>}><Compact items={data.approvals.map(x=>({id:x.id,title:x.title,meta:x.type,status:x.status}))}/></Section><Section title="Notificações" aside={<a href="/notificacoes">Abrir central</a>}><Compact items={data.notifications.map(x=>({id:x.id,title:x.title,meta:date(x.createdAt),status:x.severity}))}/></Section><Section title="Últimas decisões" aside={<a href="/decisoes">Abrir log</a>}><Compact items={data.decisions.map(x=>({id:x.id,title:x.action,meta:`${x.actor} · ${date(x.timestamp)}`,status:x.entityType}))}/></Section><Section title="Roadmap"><div className="roadmap-mini"><CheckCircle2/><div><b>V1-A · Foundation + Control Center</b><span>Estágio atual</span></div><Clock3/><div><b>V1-B · Mercado Livre Radar</b><span>Próximo estágio — não iniciado</span></div></div></Section></div></Page>}
function Compact({items}:{items:{id:string;title:string;meta:string;status:string}[]}){return items.length?<div className="compact-list">{items.map(x=><div key={x.id}><span><b>{x.title}</b><small>{x.meta}</small></span><Badge tone={statusTone(x.status)}>{x.status}</Badge></div>)}</div>:<div className="empty">Nenhum registro.</div>}
function Page({children}:{children:React.ReactNode}){return <><header className="page-head"><div><span>CENTRO DE CONTROLE</span><h1>Visão operacional</h1><p>O que exige atenção agora, sem métricas comerciais simuladas.</p></div><div className="local"><TriangleAlert size={17}/>Execução local</div></header>{children}</>}
